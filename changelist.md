# 本次改动清单

## 新增功能

### 1. 标注驱动编辑（图片节点新工具）

在图片节点悬浮工具栏里新增了"标注编辑"按钮，紧跟在"局部编辑"（蒙版）之后。点击后打开一个弹窗，可以：

- 在图片上拖拽画箭头，指向要修改的区域
- 点击放置文字标注，补充说明
- 支持撤回/重做（Ctrl/Cmd+Z、Ctrl/Cmd+Shift+Z），支持缩放平移查看细节
- 填写修改意图后提交，AI 会按标注生成一张改好的新图，放在原图右侧，原图保持不动

这是这次移植里唯一真正新增的能力，对标 Cowart 里"画箭头标注 + AI 按标注生成修订图"的交互。

### 2. DashScope（Qwen 通义万相 / Z-Image）图像模型脚本模板

在设置 → 渠道 → 编辑模型 → 调用脚本里，新增了一个"DashScope (Qwen/Z-Image)"模板选项，跟现有的 OpenAI / Gemini 模板并列。选中某个 Qwen 或 Z-Image 模型后可以直接插入这个模板，免去手写调用逻辑（内部处理了 DashScope 特有的 `W*H` 尺寸格式和响应结构解析）。

### 3. 界面文案补充

配合上面两项功能，在中英文语言包里补充了对应的按钮文字、提示语和错误提示（标注编辑相关、DashScope 模板说明）。

## 测试结果

- `typecheck`、`build` 都过，没有编译错误
- 浏览器里重新走了一遍完整流程：上传图片 → 标注编辑按钮出现 → 画箭头 → 撤回/重做状态正常 → 空输入校验报错正确 → 提交后正确跳转到配置弹窗（因为测试环境没配 API key）→ DashScope 模板在渠道编辑器里能正常插入
- 没发现任何回归或报错
